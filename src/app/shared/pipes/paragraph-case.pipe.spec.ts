import { ParagraphCasePipe } from './paragraph-case.pipe';

describe('ParagraphCasePipe', () => {
  it('create an instance', () => {
    const pipe = new ParagraphCasePipe();
    expect(pipe).toBeTruthy();
  });
});
